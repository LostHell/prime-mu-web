/**
 * Hide item damage, defense, and stats from the item card.
 * To show them, set this to false, but ensure they're calculated correctly using the formulas from the game itself.
 */
export const HIDE_ITEM_DMG_DEF_AND_STATS = true;